import { FC } from "react";
import { FilesList } from "./components/files/files-list";

const App: FC = () => {
  return (
    <div className="App">
      <FilesList />
    </div>
  );
};

export default App;
